{% extends 'admin/base.admin.html.twig' %}

{% block body %}
	<div class="flex w-full h-full justify-center items-center">
		<table class="text-center">
			<tr>
			<th >id</th>
			<th>nom</th>
			<th>contenu</th>
			<th>image</th>
			<th>actions</th>			
			</tr>

			{% for techno in technos %}
			<tr>
			<td >{{ techno.id }}</td>
			<td>{{ techno.title }}</td>
			<td>{{ techno.content | slice(0,30) }} ...</td>
			<td><img width="100px" height="auto" src="{% if 'https' in techno.image %}
			{{ techno.image }}
			{% else %}
			{{ asset('uploads/techno/'~techno.image) }}
			{% endif %}
			" alt="{{ techno.title }}"></td>
			<td>
			{# <a href="{{ path('dashboard_update_category', {id: category.id}) }}" class="text-green-500 cursor-pointer">éditer</a>
			<a href="{{ path('dashboard_delete_category', {id: category.id}) }}" class="text-red-500 cursor-pointer">supprimer</a>#}
			</td> 
			</tr>	
			{% endfor %}
		</table>	
	</div>
{% endblock %}